git config    git != github.


git config --global user.name "danny"
git config --global user.email danny121314151617@hotmail.com


git init
git status

git add (file)
git add .  // add all uncommit file


git commit -m "Commit msg"

git log x


git checkout (branch hash code)  // go to branch
git checkout master (go back to the lastest ver.)


git branch // view all branch
git branch (new branch) // create new branch for new idea new things.


git merge (branch name)  //merge all changes to master branch

xxxxx
